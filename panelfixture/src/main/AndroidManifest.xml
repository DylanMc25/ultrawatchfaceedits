<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Installed only on disposable emulators. Never packaged in the user APK. -->
    <uses-feature android:name="android.hardware.type.watch" android:required="true" />
    <application android:label="Panel test" android:testOnly="true" android:theme="@android:style/Theme.Material.NoActionBar">
        <activity android:name="com.example.ultrainfoboard.bridge.ChartTestActivity" android:exported="true" />
        <service android:name="com.example.ultrainfoboard.bridge.ChartTestService"
            android:exported="true" android:label="Test chart"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data android:name="android.support.wearable.complications.SUPPORTED_TYPES" android:value="SMALL_IMAGE" />
            <meta-data android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS" android:value="0" />
        </service>
    </application>
</manifest>
